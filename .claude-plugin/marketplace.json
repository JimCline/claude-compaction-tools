{
  "name": "jcline-claude-compaction-tools",
  "description": "Claude Code plugins by Jim Cline.",
  "owner": {
    "name": "Jim Cline",
    "email": "jamescline@outlook.com"
  },
  "plugins": [
    {
      "name": "idle-compactor",
      "source": "./",
      "version": "0.9.0",
      "description": "Runs /compact automatically when a session goes idle, timed to land just before the Anthropic prompt cache expires — or, in keepalive mode, pings the cache instead of compacting."
    },
    {
      "name": "compaction-capture",
      "source": "./compaction-capture",
      "version": "0.2.0",
      "description": "Saves each compaction summary to a file after compaction completes, so the context a session dropped is still on disk."
    },
    {
      "name": "compaction-guard",
      "source": "./compaction-guard",
      "version": "0.2.0",
      "description": "Re-states your standing directives after every compaction, so rules the summary dropped are back in context before the next turn."
    }
  ]
}
